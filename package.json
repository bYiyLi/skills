{
  "name": "personal-skills-monorepo",
  "private": true,
  "version": "0.1.0",
  "description": "Private, standards-first personal Agent Skills source repo installed via npx skills.",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "smoke:discover": "npx skills add ./skills --list"
  }
}
