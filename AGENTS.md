# Personal Agent Skills

本项目用于开发、验证和发布 Agent Skills。每个正式 Skill 都维护在 `skills/<skill-name>/`。

创建或修改 `skills/<skill-name>/` 前，必须同时使用 `skill-creator` 和本项目的 `skill-authoring-spec`（`skills/skill-authoring-spec/SKILL.md`）。如果任一 Skill 不存在或无法读取其完整指令，必须停止操作，不得自行创建或编辑 Skill。
