{
  "$schema": "../../../schemas/skill-evals.schema.json",
  "version": 1,
  "skill": "your-skill-name",
  "trigger_queries": [
    {
      "query": "Replace with a real prompt that asks the agent to apply a concrete rule or standard.",
      "should_trigger": true,
      "notes": "Should trigger because the task requires applying an explicit规范/standard."
    },
    {
      "query": "Replace with another prompt that asks for a rule-based judgment under this skill.",
      "should_trigger": true
    },
    {
      "query": "Replace with a neighboring prompt that is brainstorming rather than applying a rule.",
      "should_trigger": false,
      "notes": "Should not trigger because the task is ideation, not rule application."
    },
    {
      "query": "Replace with another prompt that belongs to a different domain or workflow.",
      "should_trigger": false
    }
  ],
  "output_cases": [
    {
      "id": "case-1",
      "prompt": "Replace with a real prompt that requires applying a concrete rule and citing the basis.",
      "comparison_mode": "with-vs-without-skill",
      "success_criteria": [
        "Applies the relevant rule or rule group instead of generic advice.",
        "Calls out any exception or ambiguity when it matters."
      ]
    },
    {
      "id": "case-2",
      "prompt": "Replace with a real prompt that tests boundary conditions or rule conflicts.",
      "comparison_mode": "with-vs-without-skill",
      "success_criteria": [
        "Uses the documented conflict-resolution path instead of inventing a new rule.",
        "Escalates or marks uncertainty when the current rules are insufficient."
      ]
    }
  ]
}
